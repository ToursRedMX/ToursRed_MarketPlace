# Bitácora técnica de ToursRed

Cada entrada es un defecto que se encontró, cómo se cerró y —lo que de verdad
vale— **cómo se encontró**. Salieron de `claude.md`, que había crecido hasta
70 KB de historias cerradas: las reglas del proyecto quedaban enterradas bajo
ellas y el archivo se volvió imposible de leer de un tirón.

Aquí están completas y sin recortar. Se consulta cuando hace falta el detalle
de un episodio concreto; lo que hay que saber SIEMPRE vive en `CLAUDE.md`, con
los patrones ya destilados.

El texto va tal cual estaba, con sus correcciones internas incluidas — varias
entradas dicen «el texto anterior decía X y era falso», y esa cicatriz es
parte del valor: enseña qué tan fácil es afirmar sin verificar.

**Ojo con las cifras.** Son las del día en que se escribió cada entrada y
varias ya no son ciertas (las líneas base de tipos, por ejemplo, bajaron
después). Para un dato vigente, mídelo; esto es registro histórico, no
estado actual.

## Índice

1. La comisión del procesador: el diagnóstico de los «17 caminos» era MALO, y lo que quedaba se cerró el 11-sep-2026 (PR #211, #213 y el de PayPal).
2. Las disputas ya están en los cinco procesadores. CERRADO el 10-sep-2026.
3. Las migraciones huérfanas ya están en el repo y el ledger cuadra; lo que sigue abierto es la llave.
4. La validación del RFC de agencia contra el SAT ya cubre los siete caminos; lo que queda es una duda sobre qué lista valida.
5. `snapshot_booking_tax` ya no cuelga de un solo hilo, pero se sigue tragando sus propios errores.
6. Las Edge Functions ya pasan por `deno check` en CI, contra una línea base de 380 errores.
7. El token de Turnstile ya no se reenvía quemado. CERRADO el 09-sep-2026 (PR #180, `b3ff4a2`).
8. La bitácora no registraba el origen de ningún evento de negocio. CERRADO el 10-sep-2026, aplicado en producción.
9. Los gastos de operacion ya se pueden capturar y entran al reporte. CERRADO el 10-sep-2026, aplicado en produccion (PR #208).
10. Generar la CLABE de un SPEI se registraba como cobro. Encontrado y cerrado el 11-sep-2026, cruzando contra la API de Stripe.
11. El IVA de la comision de Stripe ya se lee en vez de suponerse. Cerrado el 11-sep-2026.
12. La primera captura de gasto real destapo dos cosas, y ninguna era que no guardara. 11-sep-2026.
13. La comision que llega despues del asiento se perdia para siempre. Cerrado el 11-sep-2026.
14. `supabase-js` ya corre en una sola versión, y una guardia impide que vuelva a divergir. CERRADO el 10-sep-2026 (código); el despliegue va aparte.
15. `xlsx` se queda desalineado, y es una DECISIÓN, no un pendiente. CERRADO el 11-sep-2026 por Axel.
16. Los tipos del front pasaron de 428 errores a 174 y ya tienen guardia que impide que suban. CERRADO el 11-sep-2026 (PR #221).
17. Pagar un gasto: despues de registrarlo, y en parcialidades. Cerrado el 11-sep-2026.
18. `generate-signed-contract` llevaba semanas rota en el repo sin que se viera. CERRADA el 11-sep-2026 (PR #225).
19.  `insert_audit_log` exigía ser admin a todo llamador con `auth.uid()` no nulo. Esa regla es correcta para una llamada 
20.  Medidos ese día: 20 cobros liquidados con `processor_fee` en cero, $88,096.47 en total. 
21.  `20260911110000_fix_remote_lint_errors`: 16 sentencias con arreglos del linter de Supabase —añade `cobros_sin_comision` a `notification_type`, redefine `activate_featured_slot`, `auto_generate_slots_for_range`, `reserve_seats`, cuatro funciones de reporte y las dos sobrecargas de `create_conversation_with_participants`, y borra cuatro helpers sin llamadores—. 

22. Los tipos del front llegaron a CERO, y la guardia ya no tolera ninguno. 11-sep-2026.
23. La línea base de eslint llevaba cuatro días floja: 267 errores de holgura. Apretada el 11-sep-2026.
24. Los 118 `set-state-in-effect`: 58 no son bugs y decirlo importa más que el número. 11-sep-2026.
25. La llave se queda abierta por decisión, y el camino de recuperación estaba podrido. 12-sep-2026.
---

- **La comisión del procesador: el diagnóstico de los «17 caminos» era MALO, y lo que quedaba se cerró el 11-sep-2026 (PR #211, #213 y el de PayPal).** Esta línea decía «17 caminos de cobro. Congelado, no resuelto» y **la mayoría no eran bugs**. Los caminos síncronos insertan `processor_fee: 0` porque al cobrar **no conocen la comisión** — hay que preguntársela al procesador — y el webhook la rellena después. Ese es el patrón del repo, no un error. Lo que decide si hay hueco es si el webhook **alcanza** la fila que dejó el camino síncrono, y al medir la matriz completa (8 `charge_context` × 5 procesadores) salieron **tres** huecos reales, no diecisiete: **(1) MercadoPago** insertaba «si no existe» y se iba, así que el 0 se quedaba para siempre — cerrado en el #211; **(2) OpenPay** actualizaba con un bloque por contexto escrito a mano y le faltaba `featured_slot`, los tours destacados que pagan las agencias — cerrado en el #213 con un respaldo genérico por `charge_reference_id` que **no enumera contextos**, así que el próximo nace cubierto; **(3) PayPal**, que el #211 dejó marcado «sin verificar» y el #213 dio por bueno: `capture-paypal-order` sólo insertaba filas nuevas y nunca actualizaba una existente. **Medido en producción el 11-sep-2026:** 2 cobros liquidados con comisión en cero, **$6,162.44**, ambos del 22-jul — `booking_deposit` $3,999.65 (ya no se reproduce, lo arregló el #188 el 09-sep) y `payment_plan_installment` $2,162.79 (ese camino seguía igual). Cerrado extrayendo `_shared/cobrosPaypal.ts`, por donde pasan los cinco caminos de `capture-paypal-order`. **La regla al actualizar, que vale para los tres:** sólo se escribe la comisión si la nueva es > 0 y la guardada es 0 o nula — pisar una comisión buena con un cero sería peor que no tocar nada, porque los procesadores no siempre la mandan y el parseo cae en `"0"`, indistinguible de «sin comisión». **Conekta y Stripe estaban bien**, comprobado leyendo: Conekta escribe `conekta_order_id` en cada fila y su webhook la encuentra por ahí (una fila por orden, `maybeSingle()` correcto); Stripe actualiza por payment intent fuera del `if (!existingTx)`. **La red que cierra el tema de verdad es `check_cobros_sin_comision()`** (`20260911010000`): comprueba el RESULTADO en vez del camino —cobro liquidado, monto > 0, comisión en 0 pasada una ventana de gracia de 2h—, así que caza el hueco lo haya escrito quien lo haya escrito, cubre los cinco procesadores y los ocho contextos, y los que se agreguen. Deja rastro en `audit_errors` y notifica a los admins; diaria por `pg_cron`. **Lo que sigue abierto:** (a) los **~30 cobros históricos** con `net_amount` igual a `amount` —Conekta antes del 04-ago, OpenPay antes del 09-sep— siguen inflando el margen y el detector NO alerta por ellos a propósito (corte en el 11-sep, para que la guardia no se vuelva ruido): es una limpieza de datos que necesita autorización de Axel; (b) los **4 cobros con `payment_processor` NULO** destapados al medir **ya estan identificados: son Stripe** — los cuatro traen `stripe_payment_intent_id` con prefijo `pi_` de 27 caracteres y ningun id de otro procesador, y el cruce sobre las 46 filas confirma que `pi_` solo aparece en filas `stripe`; $21,560.78 entre los cuatro, todos del 21 y 22-jul. Falta el UPDATE, que es dato financiero y necesita autorizacion; (c) **`stripe_fee` es una columna muerta** —0.00 en las 46 filas—, el dato no está escondido ahí; (d) el `insert` de membresía guarda `stripe_payment_intent_id: invoice.id`, o sea un id de **factura**, así que una **disputa sobre un cobro de membresía no va a ligar** con su `payment_transaction` (`_shared/disputas.ts` busca por payment intent); la disputa se registra igual, sin reserva asociada. **Moraleja del episodio, que vale más que el arreglo:** la línea base de una guardia **no es un diagnóstico**. Contar coincidencias de un patrón y llamarlas bugs mezcló 17 hallazgos donde había 3, y eso es la peor forma de tener una guardia porque enseña a leerla como ruido. Hay que seguir cada camino hasta el final antes de escribir un número.

- **Las disputas ya están en los cinco procesadores. CERRADO el 10-sep-2026.** Esta línea decía «faltan handlers en `stripe-webhook` para disputas y payouts» y llevaba **una semana en falso**: los añadiste el 03-sep en `270d712`. Al ir a trabajar sobre ella se midió el resto y salió el hueco real: de los cinco procesadores **solo Stripe** registraba disputas. **PayPal era el peor caso porque parecía cobertura** — un `case "CUSTOMER.DISPUTE.CREATED"` cuyo cuerpo entero era `console.warn()` y `break`: ni fila, ni asiento, ni alerta. MercadoPago, Conekta y OpenPay no mencionaban la palabra, y MercadoPago además **descartaba con un `return` temprano** todo lo que no fuera `payment`, así que un topic `chargebacks` se caía sin dejar rastro. **Ahora:** `payment_disputes` es de los cinco (`processor` + `processor_dispute_id`, única **compuesta** porque los ids de disputa no son únicos entre procesadores; se renombraron las columnas `stripe_*` aprovechando que la tabla tenía **cero filas**), el efecto vive en `_shared/disputas.ts` y cada webhook solo traduce su payload. Los tres helpers de avisos salieron de `stripe-webhook` a `_shared/avisosDePago.ts`. **Ninguno de los dos módulos importa `supabase-js`**, y no es estilo: los cinco webhooks usan versiones distintas, así que un import metería una segunda copia en el bundle de la mitad — misma decisión medida que en `contextoAuditoria.ts`. Lo vigila `scripts/test-disputas.mjs` dentro de `lint`, que además **rechaza el patrón exacto de PayPal**: un `console.warn` de disputa seguido de `break`. **Lo que NO garantiza:** los mapeos de los cuatro nuevos se escribieron contra **documentación, no contra payloads observados** — hay 0 disputas en la base. Por eso `last_payload` guarda el evento entero: cuando llegue una real, el mapeo se corrige leyendo esa fila. **Y sigue sin saberse si el endpoint de Stripe está suscrito a `charge.dispute.*`**; eso solo se ve en el Dashboard de Stripe. De `payout.paid` sí consta que llega: hay 3 en `webhook_logs` (28-ago a 03-sep), todos **anteriores** al handler, que es por lo que `stripe_payouts` está vacía sin que haya bug. **Estado en producción al 10-sep-2026:** migración `20260910230000` **aplicada** con `supabase db push` (ledger 891/891, 0 solo-local, 0 solo-remoto, `Remote database is up to date`), y los **cinco webhooks desplegados** conservando su `verify_jwt = false` — los cinco están declarados en `config.toml` y coincidían con producción antes de subir. **Cómo comprobar que funciona cuando llegue la primera disputa real**, que es lo único que falta: `select processor, processor_dispute_id, status, amount, booking_id, last_event_type from payment_disputes order by created_at desc;` — y si esa tabla sigue vacía pero sospechas que hubo una, mira antes `select event_type, created_at from webhook_logs where event_type ilike '%dispute%' or event_type ilike '%chargeback%'`: si ahí tampoco hay nada, el problema es de suscripción en el procesador, no del código.

- **Las migraciones huérfanas ya están en el repo y el ledger cuadra; lo que sigue abierto es la llave.** Cerrado el 02-sep-2026 en tres pasos: (1) se reconstruyeron las **151 versiones aplicadas sin archivo** exportándolas de `supabase_migrations.schema_migrations` ([PR #120](https://github.com/ToursRedMX/ToursRed_MarketPlace/pull/120)); (2) se unificó `supabase/migrations_archive/` de vuelta en `supabase/migrations/` (695 archivos, renombres 100% sin cambio de contenido) porque el CLI **solo lee ese directorio** — mientras estuvieron archivadas, `migration list` las veía como 690 aplicadas sin archivo; (3) se marcaron como aplicadas con `supabase migration repair --status applied` las 5 versiones que existían solo como archivo local (`copper_grove`, `tight_manor`, `pale_swamp`, `fix_signed_contracts_allow_executives_upload`, `fix_staging_missing_tables_and_columns`), cuyo cambio de esquema ya estaba en producción bajo otro nombre. **Estado verificado:** `supabase migration list --linked` da 867/867, **0 solo-local y 0 solo-remoto**, y `db push --dry-run` responde `Remote database is up to date`. **Dos advertencias vigentes:** (a) el SQL reconstruido es una **exportación funcional** del arreglo `statements[]`, no el texto original — los comentarios sueltos se perdieron y el formato está normalizado — sirve para reproducir el esquema, no como registro literal de lo que se escribió; (b) **nunca corras `db push --include-all`** contra este repo: reaplicaría `users_curp_or_passport_check` (de `tight_manor`), que **5 de los 11 usuarios actuales violan**, y `users_identification_check` (de `pale_swamp`), retirado a propósito. **Lo que NO está resuelto: la llave.** Nada impide todavía que un cambio nuevo se aplique directo en Supabase sin pasar por un commit — la causa raíz de los 151. Y cerrarla no es solo «commitear el SQL»: el 02-sep-2026 las dos migraciones de LinkedIn **sí pasaron por commit y PR, y aun así sumaron 2 al desfase** (151 → 153), porque al aplicarlas la base les asignó versión propia: quedó aplicada `20260902225507` contra el archivo `20260902160000_add_linkedin_oauth.sql`, y `20260902232749` contra `20260902173000_normalize_x_provider_literal.sql`. Ese par se reconcilió renombrando los archivos a la versión aplicada, pero el mecanismo sigue intacto: **o se aplica con la versión del archivo, o se reconcilia el ledger después** (`supabase migration repair --status reverted <version-equivocada>` + `--status applied <version-del-archivo>`, o el equivalente en SQL). `scripts/check-migration-drift.mjs` audita el desfase sin credenciales: se le pasa un volcado de `select version from supabase_migrations.schema_migrations` y lista qué versiones aplicadas no tienen archivo y qué archivos nunca se aplicaron bajo su versión — los pares desalineados se leen enfrentados en esas dos listas. **Desde el 03-sep-2026 esa regla ya no está sola:** el workflow `migration-drift.yml` compara el ledger contra los archivos en cada PR a main y una vez al día, y pone el check en rojo cuando aparece una versión **aplicada sin archivo**. La otra dirección — archivo sin aplicar — queda informativa a propósito (flag `--ci` del script): es lo normal en un PR que agrega una migración, y hacerla roja entrenaría al equipo a ignorar el check, que es la peor forma de perderlo. **Distingue detectar de prevenir:** nada impide todavía aplicar un cambio sin commit; lo que hay ahora es una alarma que suena después, no una llave cerrada. **Y sí bloquea:** el 03-sep-2026 se agregaron `guardia-desfase` y `guardia-fiscal` a los checks requeridos de main, y `tipos-edge` el 03-sep-2026. **Los requeridos hoy son ocho**, releídos en la API el 10-sep-2026 y confirmando además que no hay `rulesets` que agreguen otros: `typecheck`, `netlify/toursredmx/deploy-preview`, `guardia-desfase`, `guardia-fiscal`, `tipos-edge`, **`lint`**, **`smoke`** y **`guardia-dependencias`**. Con `enforce_admins: true`, aplica también a los administradores. **`lint` y `smoke` sí bloquean**, al contrario de lo que decía aquí: este archivo afirmó hasta el 10-sep-2026 que se veían rojos sin impedir el merge, y se corrigió al toparse con un `lint` rojo que sí frenaba un PR (no se sabe la fecha en que se agregaron). **La que NO bloquea es `guardia-search-path`** — el inventario PCI la daba por bloqueante y era falso. **Y esta misma lista ya se quedó vieja una vez el día que se escribió:** decía siete, y horas después `guardia-dependencias` pasó a requerida y fueron ocho. **Y ser requerido no era lo mismo que bloquear por lo suyo:** hasta el 11-sep-2026 `typecheck` estaba entre los ocho y su paso se llamaba literalmente *"Run tsc (never blocks)"* — corria `tsc`, guardaba el log y seguia. **Desde el 11-sep-2026 ya bloquea**, contra una linea base por firma; ver la entrada del backlog. El resumen por clases se conserva, pero ya no es lo que decide. **Moraleja: la lista de requeridos se lee en la API, nunca de un documento — este incluido** — `gh api repos/ToursRedMX/ToursRed_MarketPlace/branches/main/protection --jq '.required_status_checks.contexts'`. **Al hacer requerido un check nuevo, el orden importa:** primero mergea el PR que trae su workflow y después márcalo requerido; al revés, todo PR abierto desde un main sin ese archivo se queda esperando para siempre un check que nunca va a reportar. Y ojo con `strict: false`: las ramas no necesitan estar al día con main para mergear.

- **La validación del RFC de agencia contra el SAT ya cubre los siete caminos; lo que queda es una duda sobre qué lista valida.** Cerrado el 03-sep-2026. Eran **siete** caminos, no los dos documentados: el alta por email colgaba toda la validación de `formData.regimenFiscal`, campo que la UI etiquetaba **«(opcional)»** y sin `required` — dejarlo vacío apagaba la validación entera; la edición de perfil comprobaba el veredicto dentro de `if (validateRes.ok)`, así que un 500 del PAC guardaba igual sin validar; y **las cinco altas por OAuth** (Google, Facebook, Azure, X, LinkedIn) pedían el RFC como obligatorio y **no lo validaban en absoluto**. Ahora hay un solo lugar con el criterio, `src/lib/validarRfcAgencia.ts`, que distingue «el SAT lo rechazó» de «no se pudo validar» y **falla cerrado**; el régimen fiscal es obligatorio de verdad en las seis páginas de alta; y en la edición solo se valida si algo fiscal cambió, para no atar el guardado del logo al PAC. **Lo que sigue sin verificarse:** el customer de FacturAPI valida contra el registro de RFC del SAT, mientras que `RfcACuentaTerceros` se valida contra `l_LCO`, que es **otra lista** — no está comprobado que pasar la primera garantice la segunda. Un RFC de agencia real pasó ambas en sandbox (01-sep-2026); uno inventado falló solo en `l_LCO`. Red secundaria intacta: `generate-booking-cfdi` devuelve 422 con mensaje claro si a la agencia le falta régimen o CP.

- **`snapshot_booking_tax` ya no cuelga de un solo hilo, pero se sigue tragando sus propios errores.** El literal muerto se arregló el 03-sep-2026 (`20260903035817`): el guard compara `payment_status` contra `'succeeded'` — el valor que existe según `bookings_payment_status_check` — y ya no contra `'paid'`, que no era un valor posible y hacía que el snapshot dependiera **solo** de `paid_at`. La migración lleva una aserción que falla si `'succeeded'` desaparece del CHECK. **Lo que sigue abierto es el manejo de errores.** El `EXCEPTION WHEN OTHERS` del cuerpo pone los seis campos fiscales en NULL y **deja pasar la reserva**. **Ya no se pierde el rastro, al contrario de lo que decía aquí:** además del `RAISE WARNING` —que sigue sin leer nadie— inserta en `public.audit_errors` el mensaje, el `SQLSTATE`, el id de la fila y `campos_fiscales_anulados: true`, dentro de su propio bloque para que dejar el rastro nunca pueda tumbar el cobro. Lo que sigue abierto es que: si `compute_tax_snapshot` falla, el cobro se completa sin snapshot y el CFDI sale gravado al 16% sin que nada falle. Es la misma categoría de riesgo que el literal muerto, y la red vuelve a ser el cron `check_missing_tax_snapshots`, que avisa **después** de cobrar. **No se tocó a propósito:** hacerlo fallar duro bloquearía reservas ante cualquier error transitorio, y eso es una decisión de negocio, no técnica. Al atacarlo, la pregunta no es «fallar o no fallar» sino **qué se hace visible en el momento**: distinguir error transitorio de permanente, o dejar rastro en una tabla en vez de en un `WARNING` que se pierde. **Ojo con el alcance:** el trigger es `BEFORE INSERT OR UPDATE` y se creó en `20260901064051`, posterior a la última escritura sobre `bookings` (2026-08-30) — **nunca ha disparado sobre una reserva real**, así que su comportamiento correcto está razonado y simulado, no observado en producción. **Eso ya no es cierto: medido el 10-sep-2026, ha disparado y funciona.** De las 45 reservas, las **10 posteriores al trigger** (3 al 5-sep) tienen las tres cobradas y con snapshot — 10 de 10, cero sin él. Las 31 cobradas **sin** snapshot son todas anteriores. Lo que sigue abierto es solo el manejo de errores, no si dispara. **Las 31 heredadas ya NO son una duda: se midieron el 11-sep-2026 y no hay nada que reemitir.** El texto anterior decía que «si esas 31 también eran `mixed`, se facturaron al 16% plano» y dejaba la decisión abierta. Medido: **las 31 se cobraron entre el 7-jul y el 28-ago-2026**, y la feature de tratamiento mixto nació el **1-sep** (`20260901064008`). Cuando se cobraron, **la columna `tax_treatment` no existía en `tours`**: el tour no podía estar marcado como mixto porque no había dónde marcarlo, así que facturar al 16% fue lo único posible — y el generador lo documenta así (`generate-booking-cfdi`, «NULL = cobro anterior a la feature → 16% implícito»). **El corte es perfecto y demuestra que la llave funciona:** las 31 sin snapshot van del 7-jul al 28-ago y las 10 con snapshot del 5 al 6-sep; **ninguna reserva posterior a la feature se quedó sin snapshot**. **No eran 31 con exposición, eran 17, y de un solo tour:** «Tour a Teotihuacán» (`exempt_ratio` 0.20), modificado el 3-sep, o sea **después** del último cobro. Las otras 14 son `taxable_16` y su IVA correcto coincide **exactamente** con el timbrado: exposición cero. **Lo único que queda es una pregunta para el contador, no para el sistema:** si ese tour debía facturarse mixto por su naturaleza desde julio —independientemente de lo que el software supiera—, entonces **$14,705.00** debieron ir exentos y se cobraron **$2,028.28** de IVA de más, sobre 17 CFDIs y $79,675 facturados. **Ojo al margen de esa cifra:** solo 6 de los 17 CFDIs guardan `tour_amount`, así que en los otros 11 el componente de tour se estimó con el bruto de la reserva. **Y una trampa de medición que conviene no repetir:** el primer cálculo dio $2,876.50 porque comparaba el IVA del CFDI **completo** contra el IVA del **depósito**, y el CFDI incluye cargo por servicio y extras. Se detectó porque el grupo `taxable_16` mostraba una diferencia que por definición no podía existir — cuando un grupo de control da un número que debería ser cero, la base de comparación está mal, no los datos.

- **Las Edge Functions ya pasan por `deno check` en CI, contra una línea base de 380 errores.** Hasta el 03-sep-2026 no las type-checkeaba nada: `npm run typecheck` corre `tsc -p tsconfig.app.json` y ese tsconfig tiene `"include": ["src"]`, así que las ~173 funciones de `supabase/functions/` quedaban fuera. Se detectó el 01-sep-2026 a raíz de `generate-booking-cfdi`, donde un `booking as {...}` escondía que el `.select()` no pedía `tax_treatment`. Ahora `edge-types.yml` corre `scripts/check-edge-types.mjs`, que **falla solo ante errores NUEVOS**: exigir cero habría pedido semanas de limpieza antes de tener cualquier red. La firma de cada error es archivo + código + mensaje, **sin número de línea**, porque incluirlo convertiría cualquier línea en blanco de más en decenas de falsos «nuevos» (probado: 33 errores desplazados, 0 falsos positivos). **Dos cosas que conviene saber:** (a) 79 de los 457 errores originales eran `Cannot find name 'EdgeRuntime'` — el `d.ts` oficial de Supabase declara ese namespace pero **no lo expone como global**, ni con `import` ni con referencia triple-slash (verificado con una sonda mínima), pese a que 164 funciones cargan ese import; se cubre con una declaración propia en `scripts/edge-check/globals.d.ts`, fuera de `supabase/functions/` para que no se despliegue. (b) el workflow corre en **todos** los PR a main, sin filtro de `paths`, a propósito: un check requerido que no corre deja el PR pendiente para siempre, así que el filtro lo habría vuelto imposible de exigir. Con el caché de Deno tarda ~23s. Es check requerido desde el 03-sep-2026, así que un error de tipos nuevo **bloquea el merge**.

- **El token de Turnstile ya no se reenvía quemado. CERRADO el 09-sep-2026 (PR #180, `b3ff4a2`).** Detectado el 02-sep-2026 al arreglar el 400 de captcha del login con passkey, y **reproducido en producción por Axel el 09-sep**: con la contraseña equivocada al primer intento, el segundo intento —ya con la contraseña correcta— era rechazado por el captcha. El mecanismo: los tokens de Turnstile son de un solo uso, y **Supabase los manda a `siteverify` ANTES de checar credenciales** (verificado el 02-sep contra el entorno real: dos peticiones a `/auth/v1/token?grant_type=password` con credenciales basura devolvieron `captcha protection: request disallowed (invalid-input-response)` y **nunca** un error de credenciales; `invalid-input-response` es un código de siteverify de Cloudflare, o sea que hay round-trip a Cloudflare en cada intento). Así que un intento fallido gasta el token, el widget lo deja en el estado del componente, y el reintento lo reenviaba tal cual. **El arreglo no fue exponer `reset()`**, como decía el plan original: el repo ya tenía el patrón resuelto en las tres páginas internacionales (`ExoticcaPage`, `MegaTravelPage`, `NefertariTravelPage`), que remontan el widget con `key={captchaAttempt}`. Se replicó ese patrón en los 6 consumidores que faltaban (`LoginPage.tsx` —**dos** flujos: contraseña y passkey—, `SignupPage.tsx`, `AgencySignupFormBody.tsx`, `ChangePasswordSection.tsx`, `MaintenanceAdminPage.tsx`, `ContactPage.tsx`), en el `finally` de cada intento: `setTurnstileToken('')` + `setCaptchaAttempt(v => v + 1)`. `TurnstileWidget.tsx` no se tocó. **Lección para la próxima:** antes de diseñar un fix, buscar si el repo ya resolvió el mismo problema en otro lado — aquí lo había resuelto, y la nota de "6 consumidores" era correcta aunque el grep de `key={` sugiriera que eran 9.

- **La bitácora no registraba el origen de ningún evento de negocio. CERRADO el 10-sep-2026, aplicado en producción.** Medido el 10-sep-2026 sobre los 1,400 registros de `audit_logs` desde el 25-jun-2026: **de 795 eventos de negocio, los que traen IP son CERO** — la bitácora sabía de dónde vino cada login y no de dónde vino un cobro, una cancelación o un cambio de cuenta bancaria. Además `session_id` y `correlation_id` estaban vacíos en **los 1,400**, pese a que el inventario PCI los presentaba como campos capturados. El inventario decía que el hueco eran «75 eventos `DELETE`»: eran dos ejemplos, no el hueco. **La causa no era la documentada.** No faltaba tubería: `insert_audit_log` acepta los 22 parámetros; los llamadores pasaban NULL. Y no era solo que los triggers no tengan contexto HTTP — las Edge Functions que sí lo tienen tampoco lo pasaban, salvo `record-session-event`, la única, y por eso los eventos de auth eran los únicos con IP. **El arreglo va en un solo sitio** (`20260910190000`): `insert_audit_log` es el embudo único —los 8 triggers de auditoría y las 13 Edge Functions pasan por ahí— y ahora deduce IP, user agent, sesión y correlación de `current_setting('request.headers')` y de los claims del JWT. **No se usó `set_config`**, como proponía el plan original: con pooling, un GUC de sesión se filtra a la siguiente petición que reuse la conexión y le atribuiría un borrado a la IP equivocada — peor que no atribuirlo. Los GUC que deja PostgREST son **transaccionales** y no tienen ese problema. **Lo que NO cubre:** (a) los 1,400 registros viejos, sin backfill posible; (b) las escrituras que salen de una Edge Function, donde PostgREST ve las cabeceras de la petición interna — (c) lo que no pasa por HTTP (cron, service role), que se queda en NULL a propósito. **Los dos pasos para cerrarlo:** (1) la migración se aplicó con `supabase db push` —no por API ni Dashboard— para que el ledger registrara `20260910190000`, la versión del archivo; verificado releyendo: **889/889, 0 solo-local, 0 solo-remoto**, y `db push --dry-run` responde `Remote database is up to date`. (2) Las **49 Edge Functions** que escriben en tabla auditada o llaman a `insert_audit_log` envuelven sus opciones con `opcionesConContexto(req)`, vigilado por `check-audit-context.mjs` dentro de `lint`. **Ese helper devuelve OPCIONES y no un cliente, y no es capricho:** de las 49, 30 usaban `supabase-js@2.116.0`, 13 `@2.39.6` y 6 `jsr:@2.114.0`; un helper que importara una versión metía una **segunda copia** de supabase-js en el bundle de las otras 19 — reintroduciendo dentro de una función la mezcla que el #198 acababa de ordenar entre funciones. **Esa mezcla se cerró ese mismo día** (ver la entrada de la unificación de `supabase-js`), así que la duplicación ya no podría darse; la firma se queda porque no atar un módulo compartido a una versión sigue siendo lo correcto. Ya había dado la cara con un error de tipos en `stripe-webhook`, que declara su `SupabaseClient` desde 2.39.6. **YA ESTÁ OBSERVADO EN PRODUCCIÓN, y la prueba es concreta.** El texto anterior decía «probado en CI pero no observado en producción». Revisado el 11-sep-2026 sobre los 23 eventos posteriores al arreglo: **6 traen IP y 17 no**, y ese reparto está entero explicado. **Los 17 sin IP no son un hueco:** son `UPDATE` sobre `payment_transactions` con `actor_role = 'system'` y sin email, que cambian `payment_processor` y `processor_fee` — los backfills de comisiones ejecutados por SQL, sin petición HTTP de por medio. No tienen IP porque no hay ninguna que poner, que es exactamente el comportamiento buscado. **Los 2 `ADMIN_PERMISSIONS_CHANGED` son la prueba del arreglo:** salen del trigger `trg_audit_admin_permissions`, y se comprobó que `audit_admin_permissions_change` **no pasa `p_ip_address`, ni `p_session_id`, ni `p_user_agent`** — aun así los tres llegaron. Un trigger no puede saber la IP de otra forma: `current_setting('request.headers')` y los claims del JWT funcionan en producción. **Ojo, los 4 `LOGIN` NO demuestran nada:** `record-session-event` pasa `p_ip_address` explícito y era la única que ya lo hacía antes del arreglo. Al medir conviene separar lo que prueba el cambio de lo que ya funcionaba. **El hueco real era otro: `correlation_id` estaba en NULL en los 23**, porque `insert_audit_log` solo la deduce de la cabecera `x-correlation-id` y **el front no la mandaba**. Las Edge Functions sí (`cabecerasDeContexto` la genera si no viene), así que el hueco era solo del camino navegador → PostgREST → trigger. Arreglado en `src/lib/supabase.ts`: el cliente manda una correlación **por pestaña**, guardada en `sessionStorage` —no en `localStorage`, que sería compartida por todas y no distinguiría nada— y con respaldo si `crypto.randomUUID` no está disponible o el almacenamiento está bloqueado. **Aporta lo que `session_id` no puede:** el de GoTrue es el mismo en todas las pestañas del usuario; esto separa una de otra. **Se comprobó el preflight ANTES de añadir la cabecera**, porque si PostgREST no la admitiera en `access-control-allow-headers` el CORS fallaría y se caerían **todas** las peticiones del front: responde 200 y la refleja. **Lo que sigue abierto:** los 1,400 registros viejos no tienen backfill posible; y la correlación del front **está desplegada pero aún no observada** — se comprueba con `select count(*) filter (where correlation_id is not null) from public.audit_logs where created_at > <fecha del despliegue>`, que debe dejar de ser 0 en cuanto alguien toque una tabla auditada desde el navegador.

- **Los gastos de operacion ya se pueden capturar y entran al reporte. CERRADO el 10-sep-2026, aplicado en produccion (PR #208).** El catalogo de cuentas tenia las cuentas de gasto desde el 16-may-2026 (`601.01` servicios, `601.02` operativos, `601.03` viaticos, `602` tecnologia, `603` marketing), pero **no existia tabla donde capturarlos**: los asientos solo nacian de los `source_type` automaticos, asi que un pago a Telcel o a Anthropic no podia entrar al sistema por ningun lado. El reporte maestro llevaba un aviso fijo diciendolo, para que un cero no se leyera como «no hay gastos». Ahora hay `gastos_operacion` y `gastos_recurrentes` (`20260910240000`), con permiso granular `can_manage_expenses`, asiento por `registrar_gasto_operacion` (idempotente: cargo a la cuenta de gasto, IVA acreditable si lo hay, abono a `102` si esta pagado o a `205` si no), el bloque 19 de `vista_movimientos_financieros`, y la pantalla `/admin/gastos` con carga del XML del CFDI y alta de plantillas recurrentes. **Cuatro decisiones que no son obvias:** (a) se guardan moneda y tipo de cambio, no un peso ya convertido — con MXN el tipo de cambio es 1 y un CHECK lo obliga; (b) `total_mxn` es EDITABLE porque el banco aplica su propio tipo de cambio y casi nunca cuadra al centavo con el del CFDI, y por eso el IVA en pesos se deriva PROPORCIONALMENTE del total en pesos y no como `iva * tipo_cambio`, que descuadraria el asiento en cuanto alguien ajustara el total; (c) la cuenta se valida por trigger y no por CHECK, porque la llave foranea acepta cualquier codigo del catalogo, `'102'` (Bancos) incluido, y un gasto cargado a Bancos subiria el activo mientras la vista lo sigue llamando egreso; (d) la autorizacion usa `current_user_has_role`, que excluye a los bloqueados — las politicas viejas de `accounting_entries` consultan `users` directo y por eso no lo hacen. **Y un fallo propio que salio al escribir el alta de plantillas, en `20260910250000`:** el generador de borradores insertaba `tipo_cambio = 1` sin mirar la moneda, asi que el borrador de Claude salia con 260 USD a tipo de cambio 1 y se asentaba como 260 pesos — **veinte veces menos que el gasto real**, y perfectamente CUADRADO, asi que ni el debe/haber ni el invariante `activo = pasivo + ingreso` lo cazaban. Misma clase de error que la membresia marcada como pasivo. El generador no puede saber el tipo de cambio del mes que viene y la columna no admite 0 ni NULL, asi que el borrador sigue naciendo con el relleno y lo que se prohibe es REGISTRARLO asi: CHECK `gastos_registrado_con_tipo_de_cambio_real` (tambien frena un UPDATE directo), mensaje legible en la funcion, nota en el borrador, y la pantalla deshabilita Registrar. **Moraleja que vale mas que la funcionalidad: un numero que cuadra puede ser falso, y para eso no sirve ninguna suma — hay que afirmar la clasificacion concepto por concepto y prohibir lo que no se puede afirmar.** **Verificado en produccion** releyendo tras el `db push`: las dos versiones aparecen Local = Remote sin ninguna solo-local ni solo-remota (el total del ledger no se conto), `db push --dry-run` responde `Remote database is up to date`, y la consulta de descuadre por categoria sobre `vista_movimientos_financieros` devuelve **cero filas**. **Lo que queda abierto:** (1) el permiso «Capturar Gastos de Operacion» hay que otorgarlo a mano en `/admin/users` a quien vaya a capturar — el super admin no lo necesita; (2) el lector de CFDI (`src/utils/cfdiXml.ts`) **no usa `DOMParser`** y recorre el XML a mano, porque `DOMParser` no existe en Node y `npm install` no resuelve el arbol por la dependencia de `xlsx` que se baja de un CDN bloqueado — si algun dia se arregla ese install, conviene volver al parser del navegador y borrar el recorredor; (3) ningun gasto real ha pasado todavia por el camino completo, asi que la primera captura de verdad deberia comprobarse contra el reporte maestro.

- **Generar la CLABE de un SPEI se registraba como cobro. Encontrado y cerrado el 11-sep-2026, cruzando contra la API de Stripe.** Salio de un ejercicio que no iba de esto: traer las comisiones reales de Stripe y PayPal para los cobros historicos con `processor_fee` en cero. Al pedir los 12 PaymentIntents, **dos volvieron en `requires_action` con `amount_received: 0` y `latest_charge: null`** — SPEI (`customer_balance`) donde Stripe emitio la CLABE y el dinero nunca llego: `pi_3TyfTt` $601.50 (29-jul) y `pi_3U2cet` $3,125.69 (09-ago). En la base los dos estaban `succeeded`. **La causa:** en `stripe-webhook` el insert de `payment_transactions` del anticipo estaba **fuera** del `if/else` que mira `session.payment_status`, con `status: 'succeeded'` escrito a mano. `bookings` si lo manejaba bien (`pending` / `processing` / `paid_at` nulo) y `stripe_orders` tambien distinguia `unpaid`: el unico sitio que mentia era el que alimenta el reporte. **Lo que inflaba NO era la utilidad** —esto se creyo al principio y es falso—: en `vista_movimientos_financieros` las dos filas entran como **caja +3,727.19 y pasivo +3,727.19, con `ingreso` en 0**, porque un anticipo de reserva todavia no es ingreso sino deuda con el proveedor. O sea que el reporte afirmaba tener $3,727.19 en efectivo que no existian. **Y por eso el invariante `activo = pasivo + ingreso` jamas iba a saltar: 3727.19 = 3727.19 + 0 cuadra perfecto.** Tercera vez que el mismo patron muerde — la membresia marcada como pasivo, el tipo de cambio de relleno en los gastos recurrentes, y ahora esto. **Habia ademas un duplicado latente:** `checkout.session.completed` y `checkout.session.async_payment_succeeded` comparten `case`, y ese insert no comprobaba si la fila ya existia, asi que un SPEI pagado de verdad habria dejado dos filas — y el UPDATE que rellena la comision, que filtra por `stripe_payment_intent_id` **sin `.limit`**, le habria pegado a las dos. Cero duplicados en produccion unicamente porque ninguno de los dos SPEI llego a pagarse: dano latente, no dano hecho. **Cerrado extrayendo `_shared/cobrosStripe.ts`**, por donde pasan los dos sitios de `booking_deposit` del webhook (el normal y el de cobertura insuficiente). Deriva el estado de `payment_status` e **ignora a proposito cualquier `status` que venga en la fila**, para que copiar y pegar el bloque no pueda reintroducir el 'succeeded' a mano; dedupe por payment intent con `.limit(1)` y no `maybeSingle()` (revienta con 2+ filas, y este modulo existe justo para cuando ya hay duplicados historicos); solo confirma `pending -> succeeded` y **nunca al reves**, porque un reenvio tardio del evento `unpaid` borraria ingreso real; y **no toca `processor_fee`** al confirmar, que es la misma regla que el #211 le puso a MercadoPago. `scripts/test-cobros-stripe.mjs` lo ejecuta con un cliente de mentira (11 casos) y esta en `lint`; se comprobo contra **8 mutaciones** que la prueba falla al deshacer cada pieza del arreglo — una sobrevivio al principio, porque la asercion miraba «que el texto aparezca cerca» en vez de la condicion misma del ternario. **Lo que NO se toco:** las dos filas fantasma siguen en produccion. Son datos de prueba que se van a depurar antes de las UAT, asi que limpiarlas era trabajo tirado; si alguna vez esto pasara con dinero real, el arreglo es poner esas filas en `pending`, no borrarlas. **Del mismo ejercicio salieron dos cosas mas:** (a) las comisiones reales de Stripe de los 10 cobros que si entraron suman **$1,818.56** (4.85%, es Amex) con el IVA exacto al 16% del `stripe_fee` en los 10, que encaja con `processor_fee_base` / `processor_fee_iva`; (b) un descuadre de $100.90 en una mensualidad del 16-jul resulto ser el cargo por servicio del 5% que la fila no guardaba — **ya arreglado el 27-ago en `f190ce9`** con `planTotalToPay`, la fila es anterior al fix. **El conector de PayPal devolvio 401** en todas las llamadas, asi que esa mitad del ejercicio no se pudo cerrar; y aunque se reautorice, apunta a produccion mientras esos captures son de sandbox. **Y el arreglo tuvo su propia secuela, encontrada al verificar DESPUES de mergear el #219.** El handler de `payment_intent.succeeded` hacia `if (!existingTransaction)` y se salia. Con la fila 'pending' que ahora nace correctamente, eso la dejaba en 'pending' **para siempre**: el endpoint de Stripe **no esta suscrito a `checkout.session.async_payment_succeeded`** —comprobado leyendo `enabled_events` del endpoint el 11-sep-2026—, asi que `payment_intent.succeeded` es el UNICO evento que llega cuando el dinero de un SPEI entra. O sea que el primer arreglo **cambiaba contar de mas por contar de menos**, y el codigo viejo acertaba en ese caso por accidente. Cerrado haciendo que esa rama tambien pase por `asentarCobroStripe` (confirma en vez de saltarse) y pidiendo la comision tambien al confirmar, porque la fila 'pending' nacio sin ella. De paso se le agregaron `charge_context` y `charge_reference_id`, que le faltaban: importa desde que la rama deduplica, porque si `payment_intent.succeeded` se adelanta a `checkout.session.completed` esa es la fila que se queda, y sin `charge_context` la vista arma la categoria como `'cobro_' || NULL`. **Dos cosas que quedan abiertas en la configuracion de Stripe, no en el codigo:** (1) conviene suscribir `checkout.session.async_payment_succeeded` de todos modos —el codigo ya no depende de el, pero es el evento correcto para ese camino—; (2) **la cuenta de produccion `acct_1Roc3UEakEqayEr8` (livemode) no tiene NINGUN webhook configurado** —cero endpoints—, asi que hoy todo corre sobre la cuenta de prueba `acct_1Roc3vEs5wtTyCYm`; es bloqueante antes de cobrar de verdad. **Moraleja doble, y la segunda es la que duele:** (a) ninguna suma iba a encontrar el bug original — se encontro **cruzando contra la fuente externa**, y un reporte que cuadra consigo mismo no es un reporte verificado; (b) **arreglar la mitad de un camino asincrono puede romper la otra mitad**, y eso solo se ve mirando a que eventos esta suscrito el webhook de verdad, no leyendo el codigo. Verificar despues de mergear no es paranoia.

- **El IVA de la comision de Stripe ya se lee en vez de suponerse. Cerrado el 11-sep-2026.** Salio de verificar el backfill de comisiones, no de buscarlo: al cuadrar las 13 filas de Stripe, la comision total ($2,102.80) no coincidia con base + IVA ($1,818.56) — faltaban $284.24 de tres filas del 26-ago con `processor_fee` pero `processor_fee_base` y `processor_fee_iva` NULAS. **La causa:** `getStripeProcessorFee` leia `balance_transaction.fee` —el TOTAL— e ignoraba `fee_details`, que viene en la MISMA respuesta con el desglose (`{type:'stripe_fee'}` y `{type:'tax'}`). OpenPay si llenaba las tres columnas desde el 08-sep; Stripe se habia quedado atras. **Y aqui va una correccion a lo que se dijo primero, que importa mas que el arreglo: se reporto como «ese IVA no es acreditable» y eso era FALSO.** `create_accounting_entry_for_booking` (`20260830221915`) tiene una cascada: si `processor_fee_base` es nula, deriva `base = fee / 1.16`. Comprobado contra los 13 cobros reales, esa derivacion da el centavo EXACTO en los 13 (227.34 -> 195.98, 28.45 -> 24.53, 82.74 -> 71.33). La contabilidad nunca estuvo mal. **Lo que se cerro es la DEPENDENCIA de la suposicion**, no un agujero: si algun dia Stripe cobra una comision sin IVA —una tarjeta extranjera facturada distinto—, dividir entre 1.16 acreditaria un IVA que nadie cobro. No se ha visto ni un caso, pero los 13 medidos son de prueba y todos con la misma Amex, asi que no se puede descartar. **Como quedo:** la funcion devuelve `{fee, net, base, iva}` leyendo `fee_details` **por `type` y sumando** —el orden VARIA entre cobros, observado el mismo dia, y Stripe puede partir la comision en varias lineas—, y los seis sitios que actualizan la comision pasan por `columnasDeComision()`, que **omite** las columnas del desglose cuando son nulas en vez de mandarlas: mandarlas borraria un desglose ya guardado, misma regla que «no pisar una comision buena con un cero» del #211. **`base` e `iva` vuelven NULAS a proposito** cuando Stripe no manda `fee_details` o cuando el desglose no suma el total: un valor derivado guardado en la columna es indistinguible de uno real, y la cascada contable ya cubre ese caso de forma documentada. Mejor un hueco explicito que un numero inventado. `scripts/test-desglose-comision-stripe.mjs` **ejecuta** las dos funciones recortadas del fuente (11 casos, 7 mutaciones cazadas). **Dos tropiezos propios al escribirlo, los dos por el mismo vicio:** (a) la guardia `test-comision-procesador.mjs` canto cuatro falsos positivos porque busca `processor_fee` DENTRO del `.update(` y ahora vive en el ayudante — se le enseño el nombre exacto, no «cualquier llamada», que la volveria inutil; (b) el recorte del fuente contaba llaves desde la primera `{` y emparejaba la del TIPO DE RETORNO `Promise<{...}>` en vez de la del cuerpo, justo la parte que este arreglo cambio — se corta por la llave a columna cero. **Backfill hecho:** las tres filas del 26-ago se llenaron con el desglose REAL traido de la API de Stripe, no con el derivado; 13 de 13 filas de Stripe con desglose, base + IVA = comision exacto, IVA al 16.00%. **Moraleja:** el hallazgo no salio de leer codigo ni de una guardia, salio de que una suma de verificacion no cuadro por $284.24. Y el primer diagnostico de su gravedad estuvo mal por no leer la cascada contable antes de opinar — verificar antes de alarmar vale igual que verificar antes de tranquilizar.

- **La primera captura de gasto real destapo dos cosas, y ninguna era que no guardara. 11-sep-2026.** Axel cargo el XML de una factura de TikTok, la pantalla dijo «Gasto guardado como borrador» y la lista salio vacia, con los cuatro contadores en cero. **La fila SI existia.** El lector de CFDI toma la fecha de la FACTURA —01-jul-2026— mientras el filtro de la pantalla sigue en el mes actual, y `cargar()` filtra `gte('fecha', desde).lte('fecha', hasta)` por el periodo seleccionado: el gasto estaba en julio y el usuario mirando septiembre. **Guardado correcto, pantalla mintiendo.** Arreglado moviendo el filtro al mes del gasto y diciendo por que, en vez de solo avisar: el objetivo es VERLO, y dejar al usuario cambiando el selector a mano despues de un mensaje es pedirle que arregle algo que la pantalla ya sabe. **Lo segundo es mas serio y no se veia en pantalla: `creado_por` estaba en NULL.** La columna existia desde `20260910240000` y el insert nunca la mandaba, asi que toda captura nacia sin autor — y `gastos_operacion` **no tiene trigger de auditoria**, a diferencia de `payment_transactions`, asi que esa columna es el UNICO rastro de quien metio un gasto. Importa justo desde ese dia, porque `can_manage_expenses` se le otorgo a un usuario que no es el super admin. Se cerro en dos capas: el insert de la pantalla manda el autor, y `20260911020000` pone `DEFAULT auth.uid()` en las dos tablas — **la del DEFAULT es la que vale**, porque cubre a cualquier cliente presente o futuro, script de importacion incluido; la del frontend sola se vuelve a romper con el proximo copy-paste. **Y probar la migracion contra Postgres 16 destapo un bug en el arreglo del frontend que yo mismo acababa de escribir:** se habia puesto `creado_por: autor ?? null`, y en Postgres **un NULL explicito ANULA el DEFAULT** —solo aplica cuando la columna no viene en el INSERT—, asi que el cinturon estaba cortando los tirantes. Ahora la clave se OMITE cuando no hay sesion. **No se pone NOT NULL** a proposito: rompe el generador de recurrentes y cualquier carga por service role, que no tienen `auth.uid()`; NULL ahi significa «no hubo persona», no «se desconoce». **La fila vieja no se rellena:** se capturo con la sesion de Axel pero eso NO consta en la base, y escribir un autor deducido de una conversacion seria inventar un dato de auditoria. `scripts/test-pantalla-gastos.mjs` pasa de 11 a 14 casos, con 7 mutaciones cazadas. **Moraleja doble:** (a) «no aparece» no es «no se guardo» — la primera pregunta ante un reporte de UI es si el dato esta en la base, y aqui estaba; (b) una prueba que no se ve fallar no prueba nada: una de las siete mutaciones parecio sobrevivir y resulto que el `perl` no habia aplicado el cambio (0 coincidencias), no que la asercion fuera debil. Comprobar que la mutacion se aplico es parte de la mutacion.

- **La comision que llega despues del asiento se perdia para siempre. Cerrado el 11-sep-2026.** Salio de verificar la primera captura de gasto real: al cuadrar el IVA acreditable, el libro traia $100.89 y solo la comision de Stripe escrita ese dia sumaba $290.04. **El asiento contable se publica al confirmar el pago; la comision se conoce DESPUES** —hay que preguntarsela al procesador— y para entonces la poliza ya es INMUTABLE por diseño (`'Una poliza publicada no puede eliminarse; genere una reversa'`). Nada volvia a mirarla. Medido: **12 asientos sin la comision de su cobro, $2,157.01** — $1,859.48 de gasto y $297.53 de IVA acreditable que nadie acreditaba. **Y el asiento CUADRABA**: `102 Bancos` se cargo con el BRUTO en vez del neto, asi que debe = haber y el balance tambien. Lo que estaba mal era la CLASIFICACION — dinero que nunca entro al banco figurando como si hubiera entrado. Cuarta vez que muerde el mismo patron. **Son DOS causas distintas:** (a) una CARRERA en `stripe-webhook`, que disparaba `sync-booking-to-accounting` unas lineas ANTES de consultarle la comision a Stripe — no llegaba dias tarde sino ~100 ms, y el asiento ganaba siempre porque el otro camino incluye una llamada a la API; comprobado que los otros cuatro caminos (conekta, openpay, mercadopago, capture-paypal-order) ya la escribian ANTES, este era el unico invertido; (b) comisiones que llegan tarde DE VERDAD, que el orden no salva: el asiento de PayPal `I-2026-07-0008` es del 22-jul y su comision se escribio el 09-sep con el arreglo del #188. Por eso hay dos piezas: reordenar cierra el caso comun, y `20260911030000` cierra el resto con `comisiones_no_asentadas()` + `asentar_comisiones_faltantes()`, que genera una **poliza de DIARIO** por hueco (carga 604 y 108, abona 102) en vez de tocar el original, porque el sistema lo prohibe y hace bien. `source_type='manual'` y `source_id` = el asiento original: no es estilo, `accounting_entries_source_type_check` es lista cerrada y no admite un valor nuevo, y esa pareja da la idempotencia. Corre a diario por `pg_cron` a las 14:30 UTC, una hora DESPUES de `cobros-sin-comision`, para asentar solo lo que ya tiene comision. **Un delta NEGATIVO —el libro con mas comision que el cobro— NO se corrige solo**: seria abonar a 604, o sea bajar un gasto ya registrado, y eso lo mira una persona; se reporta a `audit_errors`. **La leccion mas util no es el arreglo sino como se llego:** la consulta de deteccion salio MAL CUATRO VECES contra produccion, y ninguna version fallaba sola — cada una daba un total plausible y distinto ($2,213.91, $5,566.69, $1,681.75) antes del bueno ($2,157.01). Los cuatro errores: comparar ambitos distintos (el asiento de `booking` cubre deposito + cargo de servicio, no el cobro completo); cambiar los dos lados de la comparacion a la vez; mapear por «la transaccion mas reciente del booking» —que es lo que hace la propia funcion— y contar las mensualidades dos veces; y comparar `processor_fee_base` sin su cascada, que esta NULA en MercadoPago y Conekta y hacia parecer que diez asientos correctos tenian comision de mas. **Si se hubiera escrito el primer total, habrian quedado 25 asientos permanentes con cifras inventadas.** `scripts/test-comisiones-al-libro.sql` corre los 10 casos contra un Postgres de verdad y **las 7 mutaciones caen**; dos sobrevivieron al principio y las dos por NULL en las aserciones de la PRUEBA: `sum()` sobre cero filas da NULL y `NULL <> x` es NULL, asi que el `IF` no dispara — paso con el abono escrito a la cuenta equivocada y con la cascada quitada. **Moraleja: una asercion numerica sin `coalesce` o sin un `IS NULL` explicito no afirma nada.**

- **`supabase-js` ya corre en una sola versión, y una guardia impide que vuelva a divergir. CERRADO el 10-sep-2026 (código); el despliegue va aparte.** El PR #198 fijó las versiones flotantes pero **dejó la mezcla en pie a propósito**: quedaron cuatro versiones de `supabase-js`, ahora exactas (89 en `npm:@2.116.0`, 67 en `npm:@2.39.6`, 13 en `jsr:@2.114.0`, 5 en `npm:@2.108.2`), desde **dos registros distintos**. Eso ya había mordido dos veces: un TS2345 en `stripe-webhook`, que declaraba su `SupabaseClient` desde 2.39.6 y no aceptaba el cliente de 2.116.0, y el rediseño de `contextoAuditoria.ts` para devolver opciones en vez de un cliente. Ahora los **86 especificadores de 85 archivos** están en `npm:@supabase/supabase-js@2.116.0`: **175 usos, una versión, un registro.** **El salto de 2.39.6 a 2.116.0 asusta y no es un cambio de mayor:** 2.39.6 trae `postgrest-js@1.9.2` y 2.116.0 trae `postgrest-js@2.116.0`, pero es **renumeración, no ruptura** — Supabase alineó los cinco subpaquetes (auth-js, functions-js, postgrest-js, realtime-js, storage-js) con la versión del padre. Comprobado sobre la superficie que el repo usa: de los **27 métodos de `PostgrestFilterBuilder` en 1.9.2, en 2.116.0 no falta ninguno**, y hay 5 nuevos. Superconjunto estricto. **Lo que de verdad cierra el tema es la guardia, no el cambio:** `check-edge-deps.mjs` vigilaba una sola regla —versión exacta— y **esa regla nunca habría detectado esta mezcla**, porque 175 imports pueden llevar cada uno su versión exacta y distinta y pasar sin una queja. Se le añadió la **regla 2, versión única**: ningún paquete en dos versiones ni desde dos registros. Nace en cero (6 paquetes remotos, una versión cada uno). **Y el front también está alineado, con su propia regla.** Axel pidió que front y edge corrieran la misma versión y que fuera la última: **2.116.0 ES la última estable** (07-sep-2026; lo que viene después son prereleases de 3.0.0), así que edge ya estaba donde debía. El front no: declaraba `^2.115.0` y **el lock fijaba 2.115.0**, o sea que **producción corría 2.115.0 mientras las Edge Functions corrían 2.116.0**. Ojo con cómo se mide esto: el `node_modules` local tenía 2.116.0 y mirarlo daba «ya están alineados» — **Netlify instala desde el lock, no desde lo que tengas instalado**. Ahora `package.json` dice `2.116.0` **exacto** (sin caret, que deja la elección a npm el día del install) y el lock se regeneró con `npm install --package-lock-only`, que lo reescribe **sin descargar nada** — hay que usar ese, porque el `npm install` completo no resuelve el árbol por lo del CDN de `xlsx`. Movió exactamente 6 paquetes: `supabase-js` y sus cinco subpaquetes, otra vez en lockstep. Lo vigila la **regla 3**, que compara `package.json`, `package-lock.json` y las Edge Functions, y **rechaza también un rango en el front aunque resuelva a la versión correcta**. **Las tres reglas tienen prueba propia**, `test-guardia-dependencias.mjs`, que corre **antes** que la guardia en el mismo job — 14 casos, las tres **verificadas por mutación**: al desactivar cada una, el test falla. **Lo que la guardia NO cubre:** `xlsx` está **excluido de la regla 3 a propósito**, y desde el 11-sep-2026 eso es decisión cerrada — ver el punto siguiente. **Desplegado el 11-sep-2026: las 87, en dos tandas.** Primero las 63 sin relación con cobros, después las 24 que tocan dinero, y los dos webhooks de uno en uno. Verificado: **ningún `verify_jwt` cambió en todo el proyecto**, 87/87 ACTIVE, 87/87 arrancan y cero `boot error` en los logs. **El cruce de `verify_jwt` contra la API ANTES de desplegar no es opcional:** el CLI pone `true` a lo que no esté declarado en `config.toml`, y `stripe-webhook` y `openpay-webhook` viven de tenerlo en `false` — si se les pone `true`, los procesadores dejan de entregar eventos y los cobros no se registran. Salieron limpios porque los dos están declarados. **Y el despliegue destapó una función caída que el CLI dio por buena:** ver la entrada de `generate-signed-contract`. La lección operativa: `Deployed Functions` no significa que arranque — un `OPTIONS` a cada función después del despliegue sí lo comprueba, ejercita el arranque y no dispara lógica de negocio. **El texto anterior decía que faltaba el despliegue:** el código estaba unificado, pero en producción cada función seguía corriendo lo que se desplegó. Son **87 funciones**, no 88: el 88 salía de contar `_shared/openpay.ts`, que es un módulo compartido y no se despliega solo. Son **84 con el `index.ts` cambiado** más **tres que no aparecen en el diff**: `create-featured-slot-checkout`, `process-payment-plan-installment` y `purchase-post-booking-extras` ya estaban en 2.116.0 y aun así necesitan redespliegue, porque importan `_shared/openpay.ts`, cuyo especificador sí cambió; **las tres cobran**. De las 85, **8 mueven dinero** (`stripe-webhook`, `openpay-webhook`, `create-checkout-session`, `create-openpay-checkout`, `create-membership-subscription`, `manage-membership-subscription`, `purchase-gift-card`, `_shared/openpay.ts`) y 3 solo leen `payment_transactions`. Por eso el despliegue va **escalonado**: primero las que no cobran, el grupo de pagos después de confirmar que el primero no rompió nada. Detalle completo en `docs/pci/inventario-de-componentes-de-terceros.md`.

- **`xlsx` se queda desalineado, y es una DECISIÓN, no un pendiente. CERRADO el 11-sep-2026 por Axel.** El front seguirá con la última del CDN de SheetJS y las Edge Functions con la última que hay en npm. No se alinean porque no se puede sin cambiar de origen, y no compensa. El front usa 0.20.3 (CDN de SheetJS) y las dos Edge Functions que lo usan, 0.18.5 (npm). **SheetJS dejó de publicar en npm**: la última ahí es 0.18.5, de 2022, y `npm view xlsx@0.20.3` responde **404**. Alinearlos exigía cambiar de ORIGEN —o las Edge Functions tiran del CDN de SheetJS, o el front vuelve a una versión de 2022—, y **esa es la decisión que se tomó: ninguna de las dos.** Cada lado se queda con la última versión que su origen ofrece. Por eso `xlsx` está **excluido explícitamente de la regla 3** de `check-edge-deps.mjs` en vez de escondido: la guardia no va a volver a preguntar por él. **Lo que se aceptó al cerrarlo, y no conviene olvidar:** 0.18.5 está por debajo de **dos avisos high sin parche en npm** — GHSA-4r6h-8v6p-xvw6 (prototype pollution, < 0.19.3) y GHSA-5pgg-2g8v-p4x9 (ReDoS, < 0.20.2). **Pero la exposición práctica hoy es nula, y el matiz importa más que el aviso:** los dos se disparan al **parsear** un archivo, y **nadie en el repo llama a `XLSX.read`** — `generate-insurance-xlsx`, `send-travel-insurance-notification` y las pantallas del front solo **generan** hojas (`aoa_to_sheet`, `book_new`, `write`). Versión vulnerable, camino vulnerable no ejercitado. **Esto se cierra con una condición, y es la única que lo reabre:** el día que alguien acepte un .xlsx subido por un usuario esto pasa de deuda a urgente**, y ese cambio no avisará de que lo es: quien toque la subida de archivos tiene que acordarse de esto.

- **Los tipos del front pasaron de 428 errores a 174 y ya tienen guardia que impide que suban. CERRADO el 11-sep-2026 (PR #221).** Eran «errores preexistentes» que nadie miraba, y dentro había **dos bugs de cara al cliente**. **(1) Cobrar con Conekta fallaba:** `setPaymentModal({...})` en `TravelerBookings` reemplazaba el estado entero y omitía `conektaMethod`/`openpayMethod`, que quedaban `undefined`. Lo peor es que **no se notaba**: `PaymentProviderSelector` tiene default de prop (`conektaMethod = 'card'`), así que la UI mostraba «Tarjeta» seleccionada mientras el estado estaba vacío; al pagar, la clave desaparece en el `JSON.stringify` y `create-conekta-order` responde **400 «payment_method_type son requeridos»**. El viajero elegía Conekta, veía Tarjeta marcada y el cobro fallaba, salvo que tocara el sub-selector. OpenPay se salvó por un `method || "card"` en su Edge Function. Mismo bug en el pago directo de suplementos. **(2) El desglose de la confirmación no se pintaba nunca:** `BookingSuccessPage` leía `booking.adults_count` y `booking.adult_price` — **ninguna de las diez existe**; las columnas se llaman `count_adultos`, `count_ninos`, `count_infantes`, `count_adultos_mayores`, `count_mascotas`, y los precios no están en `bookings`. Con el guard `> 0` sobre `undefined`, el bloque entero se saltaba. De los 11 sitios del repo que tocan esas columnas, **ese era el único con los nombres en inglés**. No se arregló con el renombre obvio: el precio del tour es el de HOY y puede haber cambiado, así que sale de `booking_travelers.precio_aplicado` —lo cobrado a ESE viajero en ESA reserva— agrupado igual que en `BookingFlowStep4`. Verificado antes de escribirlo: las 45 reservas tienen viajeros, 0 sin precio y 0 sin categoría. **Lo demás fue limpieza medida:** 50 errores eran el tipo desactualizado (`points_used` sola explicaba 19) y se cerraron verificando **cada campo contra `information_schema`** — los 31 que no existen en la base **se dejaron rojos a propósito**, porque declararlos habría callado a `tsc` dejando el bug intacto; `AgencyPublicProfile` definía su propia `Agency` sin los 8 campos de dirección (hay **cuatro** definiciones de `Agency` en el repo); y 168 símbolos muertos se fueron con dos codemods sobre el AST. **La guardia: `scripts/check-front-types.mjs`, dentro de `typecheck.yml`, que deja de ser informativo.** Sustituye a `BASELINE_TOTAL`, que era un contador y por eso no servía: no ve las sustituciones (arreglas cinco, entran cinco, el total no se mueve) y se desactualiza en silencio — ese día decía 460 con el repo en 428, o sea **32 errores nuevos admitidos sin chistar**. Ahora compara por **firma (archivo + código + mensaje, sin número de línea)** contra `scripts/front-check/baseline.txt`; bajar errores no cuesta nada, subirlos pone el check en rojo. **La base NO nace en cero (arranca en 174) y está bien:** exigir cero pedía semanas de limpieza antes de tener red; para bajarla, `--update`. Tiene prueba propia (`test-guardia-tipos-front.mjs`, 7 casos) que corre **antes** que la guardia e incluye el caso que el contador no podía ver: **un error resuelto y otro nuevo con el total idéntico**. **Tres trampas que costaron y conviene no repetir:** (a) comparar con número de línea da falsos «nuevos» — al añadir campos a `types/index.ts` salieron 10 que solo se habían desplazado, la misma lección ya escrita en `check-edge-types.mjs`; (b) en Windows `tsc` emite CRLF y en JavaScript el `.` **no casa `\r`**, así que un patrón terminado en `(.*)$` no casa ninguna línea y el script habría concluido «no hay errores de tipos» — lo cazó la comprobación de que el chequeo de verdad corrió; (c) el codemod de imports rompió la sintaxis de 8 archivos y luego de 3 más, porque **cortar especificadores uno a uno se solapa cuando dos muertos son adyacentes** (`Check, CheckCheck`) — se arregló reconstruyendo la lista de vivos, y lo que evitó el desastre fue que el codemod **parsea cada archivo antes de escribirlo** y descarta el que no compile. **Lo que queda abierto:** 174 errores, de los cuales 57 son símbolos sin usar que piden juicio (funciones muertas de 10-20 líneas, y 15 destructuring donde mueren TODOS los bindings — ahí no es quitar un nombre, es decidir si la llamada debe seguir ocurriendo). Y los 31 rojos a propósito de `BookingSuccessPage` siguen señalando que esa pantalla fue escrita contra un esquema que no existe.

- **Pagar un gasto: despues de registrarlo, y en parcialidades. Cerrado el 11-sep-2026.** Axel pregunto «si ya estaba pagada, como genero el asiento del pago». **El campo `Pagado el` SI existe en la captura** —si se llena, el asiento abona directo a `102 Bancos` y no hay segundo paso— pero lo demas faltaba entero: la pantalla solo ofrece «Editar» en estado borrador y `registrar_gasto_operacion` sale temprano si el gasto ya esta registrado, asi que **no habia camino para marcar pagado un gasto ya asentado**; y `pagado_en` es una fecha, o sea que no sabe decir «van 100 de 232». **Y un comentario que yo mismo escribi en `20260910240000` afirmaba que «cuando se pague, se edita el gasto con su `pagado_en` y el asiento cambia de '205' a '102'». Es FALSO** — una poliza publicada es inmutable — y peor: el atajo que sugiere separa la vista del libro, porque el bloque 19 decidia caja contra pasivo con ese campo. La vista diria «salieron 232 del banco» mientras el asiento sigue diciendo «se debe». Quinta vez que muerde el mismo patron. **Como quedo (`20260911040000`):** `pagos_de_gasto` guarda un renglon por pago con su propio asiento (205 D / 102 H), `pagado_en` pasa a ser DERIVADO —lo escribe la funcion cuando la suma alcanza el total, nunca a mano— y el bloque 19 calcula `caja = -sum(pagos)` y `pasivo = total - sum(pagos)`, que es lo unico que sabe representar un parcial; el invariante se sostiene igual porque `-pagado = (total - pagado) + (-total)`. **La parte menos obvia:** el gasto capturado YA PAGADO tambien inserta su fila, apuntando al MISMO asiento y sin generar uno segundo — ese camino abona directo a 102 y nunca crea deuda, pero si no dejara rastro la vista lo veria como no pagado, porque ahora suma pagos. El modelo tiene que ser uniforme o el reporte miente. La tabla **no tiene politicas de INSERT/UPDATE/DELETE a proposito**: los pagos solo entran por `pagar_gasto_operacion`, que es SECURITY DEFINER y crea el asiento en la misma transaccion; un INSERT suelto dejaria un pago sin respaldo contable. Se rechaza el sobrepago (dejaria 205 en deudor, o sea que el proveedor te debe a ti), pagar un borrador, pagar uno cancelado, y una fecha anterior al gasto. **La vista se recrea entera (307 lineas) y se extrajo del archivo con un script en vez de transcribirla:** copiar 307 lineas a mano para cambiar dos es la forma mas facil de meter un error invisible. `scripts/test-pagar-gasto.sql` corre 10 casos sobre la CADENA COMPLETA de migraciones —no sobre un esquema inventado— y caza sus 7 mutaciones; `test-pantalla-gastos.mjs` sube a 16 casos con 5 mutaciones mas, entre ellas que el modal proponga el total en vez del saldo (invita al sobrepago) y que los totales vuelvan a partirse por `pagado_en`. **Dato de proceso:** el contador local de `tsc` bajo de 432 a 178 sin que nadie arreglara nada — es el entorno, no el codigo. Lo unico que significa algo es el delta contra main medido en el mismo momento, y fue 0. Citar una linea base vieja como si fuera actual es otra forma de afirmar sin verificar.

- **`generate-signed-contract` llevaba semanas rota en el repo sin que se viera. CERRADA el 11-sep-2026 (PR #225).** Importaba `npm:pdfmake@0.2.20/js/printer.js`, y esa carpeta **no existe** en el paquete: 0.2.20 trae `build/` y `src/`. El runtime moría con `worker boot error: Unable to load .../pdfmake/0.2.20/js/printer.js: path not found`. **Por qué no se notaba:** el import quedó así al fijar las versiones flotantes (PR #198, `0.2.x` → `0.2.20`), pero la función seguía sirviendo una compilación **anterior** a ese cambio; nadie la redesplegó hasta el 11-sep, y al redesplegarla el código del repo se compiló por primera vez y falló. Es la misma clase de desfase que el repo persigue con las migraciones —lo desplegado y lo commiteado no eran lo mismo— pero del lado de las Edge Functions, y ahí no hay ledger que lo delate. El arreglo es importar sin subruta (`npm:pdfmake@0.2.20`), cuyo default **es** la clase `PdfPrinter`: el patrón que ya usaban `approve-agency-documents` y `verify-contract-otp`. **Cómo se cazó, que es lo que vale para la próxima:** no por el despliegue —el CLI respondió `Deployed Functions` tan tranquilo— sino por un `OPTIONS` a cada una de las 63 funciones desplegadas: 62 con 200 y esa con **503 BOOT_ERROR**. El preflight ejercita el arranque **sin disparar lógica de negocio**, así que sirve de prueba de humo sin efectos secundarios; conviene hacerlo siempre después de desplegar. **Ya hay guardia, y hubo que descubrir cómo:** `scripts/check-edge-subpaths.mjs`, dentro de `edge-types.yml`. Lo difícil no fue escribirla sino encontrar algo que detectara el fallo, porque **casi nada lo detecta** — comprobado uno por uno el 11-sep-2026: `check-edge-deps.mjs` pasa (la versión SÍ es exacta y única), `deno check` sale con 0, `deno check --all` también, `deno cache` **también** (y encima descarga el paquete), y `deno info` describe el módulo sin mirar si el archivo existe. El único que falla es `deno run`, que no sirve: ejecutaría la función entera. **La solución son dos mecanismos distintos, porque se comportan distinto:** para `jsr:` basta `deno cache`, que sí falla y además lista los exports válidos; para `npm:` hay que bajar el paquete y **mirar el archivo dentro del cache** (`deno info --json` da la ruta de `npmCache`). Un solo mecanismo habría dejado medio hueco abierto sin que se notara. No ejecuta código de terceros: solo descarga y mira el disco. **Un intento anterior con `import.meta.resolve` daba falsos OK** y conviene saber por qué: cuando el paquete no está en el grafo del módulo, devuelve el especificador tal cual (`npm:...`) en vez de una ruta, y eso se leía como «resolvió, luego existe». Tiene prueba propia (`test-guardia-subrutas.mjs`, 7 casos) que corre **antes** que la guardia, y cuyo primer caso es **literalmente el import que tumbó la función**.

- **La bitácora rechazaba los eventos de los usuarios que no son admin. CERRADO el 11-sep-2026 (PR #234), aplicado en producción.** `insert_audit_log` exigía ser admin a todo llamador con `auth.uid()` no nulo. Esa regla es correcta para una llamada **directa por RPC** —sin ella cualquiera se inventaría entradas de auditoría— pero se aplicaba también cuando el llamador no era el usuario sino un **trigger** auditando su acción: el viajero que crea una reserva no es admin, así que la excepción tumbaba el registro. **Las reservas creadas por viajeros no quedaban en la bitácora**; solo las de admins y las de Edge Functions con service role. Es Requisito 10 de PCI DSS. **Cómo apareció, que es lo que importa:** `public.audit_errors` tenía **31 filas y las 31 eran la misma** —acción `BOOKING_CREATED` sobre `bookings`, del 24-ago al 05-sep-2026, mensaje «Acceso no autorizado»—. Llevaba **semanas dejando constancia de su propio fallo y nadie la leía**; salió al revisar esa tabla por curiosidad, no por una alerta. **El arreglo no abre nada:** se suma `pg_trigger_depth() = 0` a la condición. Esa función solo devuelve más de cero **dentro de un trigger**, y a un trigger no se le pasan argumentos desde el cliente: los pone la tabla. Así, la llamada por RPC de un usuario normal **sigue rechazada** y el trigger que lo audita pasa. **La migración (`20260911100000`) parte del archivo del repo, no de un volcado de la base**, y antes se comprobó que ninguna migración posterior redefine la función —la única que la nombra es `20260911020000`, en un comentario— para no revertir por accidente el trabajo de otra sesión. Lleva aserción propia: si alguien reescribe la función y se come la condición, la migración falla. **Verificado tras aplicarla:** la función viva contiene `pg_trigger_depth() = 0`, el ledger registró **`20260911100000`, la versión del archivo** (no una inventada por la base), y hay **cero versiones raras** del día. **Lo que falta observar:** la prueba real es una reserva creada por un viajero. La línea base para compararlo es que `audit_errors` tenía **31 filas, la última del 5-sep**: si crece, el arreglo no cubrió ese camino; si aparecen `BOOKING_CREATED` posteriores al 11-sep, está cerrado. **Y queda un pendiente que este hallazgo destapa:** nadie mira `audit_errors`. Dejar rastro no sirve si el rastro no dispara nada — le falta una alerta como la de `check_cobros_sin_comision`.

- **Los 20 cobros sin comisión se descartan: son pruebas, no dinero. DECISIÓN de Axel, 11-sep-2026.** Medidos ese día: 20 cobros liquidados con `processor_fee` en cero, $88,096.47 en total. **No se van a corregir** porque son datos de pruebas viejas y el ambiente se va a depurar antes de las UAT. **Lo que conviene tener presente al leer otras entradas:** si esos cobros son de prueba, también lo son buena parte de las 45 reservas y de los CFDIs timbrados, así que las cifras fiscales que aparecen en el backlog —incluidos los $2,028.28 de IVA de las 17 reservas de Teotihuacán— describen datos de prueba, no exposición real. La conclusión de aquella medición no cambia (no había nada que reemitir), pero su peso sí. **Y afecta a una guardia:** `check_cobros_sin_comision` tiene el corte en el 11-sep justamente para no alertar de los históricos; cuando se depure el ambiente, ese corte deja de hacer falta y conviene revisarlo en vez de dejarlo ahí para siempre.

- **Una migración volvió a llegar a la base sin pasar por un commit, y la guardia la cazó el mismo día. 11-sep-2026.** `20260911110000_fix_remote_lint_errors`: 16 sentencias con arreglos del linter de Supabase —añade `cobros_sin_comision` a `notification_type`, redefine `activate_featured_slot`, `auto_generate_slots_for_range`, `reserve_seats`, cuatro funciones de reporte y las dos sobrecargas de `create_conversation_with_participants`, y borra cuatro helpers sin llamadores—. **Apareció sola, y ahí está el valor:** `migration-drift.yml` puso en rojo un PR que no tocaba migraciones, porque compara el ledger contra los archivos y vio una versión aplicada sin archivo. Es el mismo mecanismo que dejó 151 huérfanas, con una diferencia que importa: **esta vez se detectó el mismo día, no meses después**, y el autor la commiteó (`eafc929`) en lugar de tener que reconstruirla. **Lo que demuestra, y lo que NO:** la guardia funciona y bloquea de verdad —`guardia-desfase` es check requerido—, pero **sigue siendo una alarma, no una llave**: nada impidió que el cambio se aplicara. La causa raíz del backlog —«nada impide todavía que un cambio nuevo se aplique directo en Supabase sin pasar por un commit»— sigue abierta, y este caso es la prueba de que se sigue usando ese camino. **Un aviso para la próxima vez que haya que reconstruir una:** se intentó recuperar el SQL del arreglo `statements[]` antes de saber que el autor iba a commitearlo, y salió que **el exportador corta por `;`**. `auto_accept_expired_slot_reschedules` los tiene dentro del cuerpo, así que su bloque `DECLARE` salía troceado en fragmentos separados por `;` en líneas sueltas: **sintácticamente roto**. Copiar una exportación tal cual produce un archivo no ejecutable en cuanto hay funciones largas — hay que recomponerlas a mano, y conviene contar los delimitadores `$function$` para comprobarlo.

- **Los tipos del front llegaron a CERO, y la guardia ya no tolera ninguno. 11-sep-2026.** El 11-sep-2026 por la mañana la línea base eran **72 errores en 57 firmas**; al cerrar el día son **0**, y `scripts/front-check/baseline.txt` quedó vacío, así que la guardia pasó de «sin errores NUEVOS» a **exigir cero**: cualquier error de tipos que entre al front bloquea. (CLAUDE.md decía «102 errores en 78 firmas» — era una cifra vieja, corregida al medir.)

  **Lo que importa no es el número: nueve de los 72 eran bugs de verdad**, y ninguno se habría encontrado leyendo el código a ojo, porque todos compilaban y ninguno lanzaba en el camino feliz. El tipo era el único testigo:

  1. **`BookingFlowStep1` anunciaba «$1.00» como precio de preventa.** `tours.preventa_precio_especial` es **BOOLEAN** en la base —dice si el tour tiene precio especial, no cuál es—, y la tarjeta que invita a comprar la membresía lo pasaba por `formatCurrencyMXN`: `Number(true)` es 1. Los otros tres sitios que usan ese campo (PreventasSection, BookingForm, TourCard) lo tratan bien, aplicando `preventa_descuento_valor` según `preventa_tipo_descuento`. Se copió esa fórmula; no se inventó una quinta.

  2. **El KPI de viajeros del panel de agencia era 0 siempre.** Sumaba `booking.travelers_count` y el `select` no pedía esa columna.

  3. **El botón «Reintentar» de ese mismo panel** hacía `onClick={fetchAgencyData}`, o sea que le pasaba el evento del ratón como `agencyId`.

  4. **Borrar una clave de acceso no funcionaba.** `supabase.auth.passkey.delete(passkeyId)` con el id suelto: la librería lee `params.passkeyId`, que sobre una cadena es `undefined`, así que la petición salía hacia `DELETE /passkeys/undefined`. Espera `{ passkeyId }`.

  5. **Y el nombre de la clave se descartaba en silencio:** `registerPasskey` no acepta `friendlyName` —su único parámetro es `{ options?: { signal } }`—. El nombre se pone después, con `passkey.update`.

  6. **Toda notificación de ticket de soporte mandaba a `/traveler/soporte`, también la de un admin.** `NotificationBell` leía `role` del contexto y `AuthContext` no expone `role`: expone `userRole`, `isAdmin`, `isAgency`. La comparación era contra `undefined` y siempre caía en la rama final.

  7. **Duplicar un tour desde la lista de tours finalizados** llamaba `handleDuplicate(tour.id)` en vez de `handleDuplicate(tour)`: el modal se abría con «undefined (Copia)» y sin fechas. El otro llamador, casi doscientas líneas más abajo, sí pasaba el objeto.

  8. **`resetForm` de `AgencyTours` no reseteaba siete campos** —las cuatro políticas de cancelación de receptivo y los flags de cancelación, cambio de nombre y mapa de vehículo—, y los siete van directo al `insert`: crear un tour después de editar otro heredaba sus políticas.

  9. **«Cancelar» en el perfil de agencia no cancelaba del todo:** reconstruía 11 de los 26 campos del formulario, así que RFC, razón social, régimen fiscal, domicilio y datos bancarios se quedaban con lo tecleado. Había dos copias del mismo objeto y la segunda se quedó atrás; ahora hay una función, `formularioDesdeLaAgencia`.

  **El patrón que explica ocho de los nueve: un objeto escrito a mano en dos o tres sitios, y uno de ellos se quedó viejo.** `resetForm`, «Cancelar», y los cuatro modales que se reemplazan enteros dejando un campo fuera (así arrastraban el valor del uso anterior). Y sobre todo `AdminUsers`, que tenía **TRES copias a mano de la lista de permisos** y entre las tres faltaban cinco. Ese importa más que los otros porque el `{ ...base, ...user.permissions }` deja en `false` toda clave ausente: una copia desactualizada no es un descuido cosmético, es un permiso que se apaga al guardar. Ahora hay una sola lista, `PERMISOS_EN_CERO`.

  **Al hacerlo se rompió `test-alta-de-contadores.mjs`**, que afirmaba sobre el literal dentro de `startEditPermissions`: la guardia estaba bien y el código cambió debajo. Se adaptó para mirar la constante **y** para exigir que `startEditPermissions` siga partiendo de ella, porque si alguien le vuelve a escribir una lista propia, la constante quedaría correcta y sin efecto. Las dos mutaciones se probaron y las dos caen.

  **Hallazgo aparte, sin arreglar a propósito:** `can_manage_service_desk` y `can_manage_executives` existen en `admin_permissions`, los leen `NavBar` y `AuthContext` para dar paso a `/admin/ejecutivos` y a la mesa de servicio, y **una de las dos filas de staff los tiene encendidos** — pero `/admin/users` ni los muestra ni los guarda: hoy solo se conceden con SQL. No los pisa (el `upsert` no los escribe), así que no hay daño; falta la casilla. Se dejaron **leyéndose** para que el estado no los invente en `false`.

  **Dos piezas de código muerto que el tipo delató:** el bloque «Método de pago» de la ficha de comisiones de ejecutivos leía `selected.payment_method`, y `executive_commissions` **no tiene esa columna** (sí la tiene `commission_records`), así que nunca pudo pintarse; y un `<Field ... highlight />` en AdminBookings pasaba una prop que `Field` no declaraba, así que React la descartaba y el «Total pagado» del plan de pagos no resaltaba.

  **La familia más numerosa no eran bugs:** una veintena venía de que **supabase-js infiere TODA relación anidada como arreglo** cuando el proyecto no tiene los tipos generados de la base, aunque PostgREST devuelva un objeto para las relaciones a-uno. Cada caso se comprobó **contra la base, mirando la dirección de la llave foránea** antes de castear —si la FK sale de la tabla que consultas, el embebido es a-uno—, y el casteo vive ahora en un solo sitio documentado, `src/lib/relacionesSupabase.ts` (`comoFila` / `comoFilas`), con la consulta de comprobación escrita ahí mismo. **El día que se generen los tipos de la base, borrar ese archivo debería dejar el `tsc` en verde solo.**

  **Tres tipos duplicados**, que TypeScript señala con un mensaje que parece un error suyo y no lo es — «Type 'Tour' is not assignable to type 'Tour'»: `DeparturePoint` tenía tres definiciones; `Tour` dos (una copia local de 11 campos en `AgencyPublicProfile`, mientras la consulta hace `select('*')` y el resultado va a `<TourCard>`); y `TourSlot` sigue teniendo dos a propósito (el del flujo de reserva es un subconjunto legítimo, lo que fallaba era importar el que no era). `TourOptionalService` era el caso contrario: `BookingFlowStep3` lo importaba de `../../types`, donde nunca se exportó, mientras vivía suelto dentro de `BookingForm`.

  **Cómo se verificó, que es lo único que hace creíble el número:** cada tanda se comparó contra la línea base por **firma = archivo + código + mensaje, sin número de línea** (incluirlo convierte una línea en blanco de más en decenas de falsos «nuevos»), y en las diez tandas salieron **0 firmas nuevas**. Al cierre: `tsc` en 0 con salida 0, `vite build` compila, las 39 guardias de `lint` corren con los mismos 2 fallos que ya trae main (`summarize-lint` necesita el `eslint-report.json` que genera CI, y `test-tax-snapshot-sql` falla por diseño), y eslint baja de 1514 a 1513. La guardia de tipos se mutó en los dos sentidos: con un error inyectado sale en rojo y al quitarlo vuelve a verde — **una base en cero sin esa comprobación no distingue «corrió y no halló nada» de «no corrió»**.

  **Un tropiezo que vale la pena dejar escrito:** al comprobar que el `tsc` de verdad corría se inyectó un error a propósito y se deshizo con `git checkout src/types/index.ts`, que se llevó por delante **los cambios sin commitear de ese mismo archivo**. Se detectó al releer y se rehicieron. `git checkout <archivo>` no distingue tu mutación de tu trabajo.

- **La línea base de eslint llevaba cuatro días floja: 267 errores de holgura. Apretada el 11-sep-2026.** `lint` es un check requerido y bloquea cuando el total de errores sube sobre `BASELINE_ERRORS`. Esa cifra se fijó en **2388** el 07-sep y no se volvió a bajar, mientras el conteo real caía a **2121**: había sitio para **267 errores nuevos sin que el check se pusiera rojo**. Una base floja no avisa de nada, y lo peor es que se ve verde.

  **Y no es teórico: por ese hueco ya se colaron dos.** Al reajustar se vio que dos reglas estaban **por encima** de su base por-regla — `react-hooks/set-state-in-effect` 117 → 118 y `no-useless-assignment` 16 → 17 — y ninguna hizo ruido, porque el total bajaba por otro lado. **Se midieron contra `origin/main` para no atribuírselas al PR que las encontró:** main tiene 2210 y la rama 2209, así que las dos venían de main.

  Ahora `BASELINE_ERRORS` es 2121 / `BASELINE_TOTAL` 2209 / `BASELINE_FILES` 316, y la tabla por regla de `scripts/summarize-lint.mjs` se reajustó a la realidad: decía `no-unused-vars: 310` cuando ya eran **79**, y `no-explicit-any: 1648` cuando eran **1615** — 264 de holgura solo entre esas dos. **La cifra se tomó de la medición de CI** (job 103441033110 del PR #242), no de escritorio: la instalación de `xlsx` difiere entre entornos y un conteo local no es comparable.

  **Lo que sigue abierto, y es una decisión de diseño, no un olvido:** el corte que BLOQUEA es el **total**, no la fila. Un PR que añada un `set-state-in-effect` —que marca un bug de React de verdad, no estilo— y a cambio borre dos `any` **pasa en verde**. Bloquear por regla con la base exacta volvería roja cualquier refactorización que cambie una regla por otra, así que el punto medio razonable sería bloquear por regla **solo en las `react-hooks/*`**, que son las que señalan comportamiento, y dejar el total para las de estilo. Queda anotado sin implementar: cambia cómo bloquea un check requerido para todo el mundo.

  **La foto de la deuda de lint, medida el 11-sep-2026:** 2209 problemas (2121 errores, 88 avisos) en 316 archivos — **1513 en `src/`, 695 en `supabase/functions/`**. El **73% es un solo patrón**: 1615 `no-explicit-any`. Después, 171 `no-useless-escape` (regex), 118 `set-state-in-effect`, 84 `exhaustive-deps`, 82 `immutability`, 79 `no-unused-vars`. Concentración: `TravelerBookings.tsx` 211, `send-inquiry-email/index.ts` 170, `AgencyBookings.tsx` 111, `src/lib/supabase.ts` 100, `AgencyTours.tsx` 87. **Los `any` no se tocan a bulto:** cada uno pide saber qué devuelve la consulta, que es justo el trabajo que hizo aparecer nueve bugs en los tipos (entrada 22).

- **Los 118 `set-state-in-effect`: 58 no son bugs y decirlo importa más que el número. 11-sep-2026.** `react-hooks/set-state-in-effect` es la única familia de eslint que marca comportamiento y no estilo, así que parecía el siguiente objetivo obvio. **Al leerlos, el reparto cambió el plan:**

  - **58 son `useEffect(() => { fetchX(); }, [dep])`.** La regla no señala un `setState` literal: traza hasta la función llamada y encuentra el `setLoading(true)` con el que abre todo cargador del repo. Es el patrón de carga de datos de la aplicación entera. «Arreglarlos» significaría reestructurar cómo cargan datos ~40 pantallas: **riesgo alto, valor cero.** No se tocan.

  - **51 son un `setState` directo en el cuerpo del efecto.** Ahí es donde vive lo que sí importa, y tampoco todos.

  - **9 son llamadas a otras funciones**, caso a caso.

  **Se cerraron 10, y dos eran defectos de verdad:**

  1. **Las tres pantallas de soporte ocultaban el bloque de adjuntos cuando no debían.** `allowAttachments` era estado sincronizado por un efecto que **solo corría si había subcategoría**: al elegir una que prohíbe adjuntos y cambiar después de categoría —lo que vacía la subcategoría— el efecto no volvía a correr y el valor se quedaba en `false`. El bloque seguía oculto hasta elegir otra subcategoría. Calculado en el render, el estado obsoleto **no puede existir**. Lo mismo con `filteredSubs`.

  2. **`AccountCatalogModal` podía guardar un código de cuenta duplicado.** `codeTaken` era estado sincronizado por un efecto cuyo comentario decía «(debounced)» — **no lo estaba, no había ningún temporizador**. Al ser estado quedaba una ventana de un render en la que `code` ya había cambiado y `codeTaken` valía lo anterior, y esa variable gobierna el `disabled` del botón de guardar **y** la validación de `handleSubmit`. Calculado en el render la ventana desaparece.

  **Y tres pantallas de agencia** (`AgencyFinancials`, `AgencyDiscountCodes`, `AgencyStaff`) guardaban `agencyId` en un `useState` que solo copiaba `resolvedAgencyId` en un efecto, sin que nadie más le escribiera: un render de más y la primera carga retrasada un ciclo. Es un alias, no estado.

  **Lo que se movió, y por qué no bastaba con calcular:** los efectos hacían dos cosas de verdad —limpiar la subcategoría al cambiar de categoría, y tirar los adjuntos al elegir una subcategoría que no los admite—. Eso pasó a los `onChange`, que es donde ocurre el suceso. **En el efecto dependían además de `subcategories`**, así que una recarga de esa lista habría borrado la selección del usuario sin que él tocara nada: latente hoy porque la lista se carga una sola vez.

  **Lo que se dejó a propósito, que es la parte que conviene no perder:**

  - **Los seis `setFormData(savedData)`** que restauran un borrador de `localStorage` dependen de `useFormPersistence`, y su `loadFromStorage` no existe antes del `useState` que lo alimenta. Convertirlos a estado inicial perezoso obliga a cambiar la API de un hook compartido en todos sus consumidores. El coste real de dejarlos es **un render de más al restaurar un borrador**.

  - **`ProtectedRoute`** es un temporizador deliberado con 300 ms de gracia antes de redirigir; volverlo derivado quitaría esa gracia.

  - **`MessagingPage`, `BookingFlowStep2` y `SignupPage`** son **estado sembrado, no derivado**: el valor entra de la URL o del contexto y después el usuario lo cambia. Convertirlos rompería comportamiento. `BookingFlowStep2` además está en el camino del dinero.

  - **Una decena de `setError('ID no encontrado')`** en guardas de salida temprana de las pantallas de éxito y pendiente. Son la rama de error, no el camino feliz.

  **Medición:** 118 → **108**; total de eslint 2209 → **2199** (2121 → 2111 errores). **Ninguna otra regla se movió** — se comprobó expresamente que `exhaustive-deps` siguiera en 84, porque retirar efectos es justo lo que suele crear dependencias nuevas. Las dos bases (`BASELINE_*` en `lint.yml` y `BASE` en `summarize-lint.mjs`) se bajaron en el mismo PR, como pide el propio script.

  **La lección, que vale para la próxima guardia que se mire:** el número de una regla no dice cuántos bugs hay. De 118 avisos reales y bien emitidos, **58 describen el estilo de carga de datos del repo** y dos eran defectos que se ven en pantalla. Contar los 118 como deuda a pagar habría llevado a un refactor grande y peligroso; leerlos uno a uno costó una tarde y dio dos arreglos. Es el mismo error que convirtió 3 huecos de comisión en «17 caminos rotos» (entrada 1).

- **La llave se queda abierta por decisión, y el camino de recuperación estaba podrido. 12-sep-2026.** La entrada 3 dejaba «la llave» abierta y bloqueada en una respuesta de Axel: cuánta gente tiene acceso al Dashboard. La respuesta del 12-sep-2026 fue **los cinco agentes (Claude Code, Claude, ChatGPT, Codex y Bolt) y él**. Y con eso el remedio anotado se cae solo: «restringir quién puede correr SQL en el Dashboard» se escribió imaginando un equipo de personas a quienes quitar acceso — hay **una** persona, así que restringir *es* quitárselo a los agentes. **Axel decidió dejarlo abierto** y reconciliar el repo cuando el ledger se desfase. Decisión tomada; no se reabre.

  **Antes de eso, el diagnóstico se midió y estaba viejo en lo esencial.** Sobre el ledger completo: **912 pares `local == remoto`, 0 desalineados, 0 archivos sin aplicar**. El mecanismo que produjo las 151 huérfanas —la base asignando su propia versión al aplicar desde el Dashboard— **ya no se reproduce**. Lo que se repite hoy es otra cosa: **el cambio llega a producción antes de que el PR se mergee**. Siete veces el 11-sep. Se auto-cura al mergear, así que no deja desfase permanente; lo que deja es esquema de producción sin revisar, y `guardia-desfase` roja en PRs ajenos — que es la forma de perder una guardia: enseñar a leerla como ruido. **Quien se tope con ese rojo debería mirar primero si su rama está desactualizada.**

  **Y al ir a documentar el camino de recuperación salió que estaba roto.** `scripts/export-orphan-migrations.sql` lleva **incrustada** la lista de versiones que ya tienen archivo, para pedirle a la base solo las que no. Se escribió a mano el 02-sep-2026 con **716**. El 12-sep el repo tenía **912**. Con esa lista, la reconciliación —el plan B que Axel acababa de elegir como red de seguridad— habría declarado huérfanas **200** migraciones, **198 de ellas con archivo**, e `import-orphan-migrations.mjs` las habría sobrescrito con su exportación mecánica, perdiendo la documentación escrita a mano de cada una. Su propia cabecera avisa de ese riesgo; lo que no tenía era nada que impidiera llegar a él.

  **Medido en los dos sentidos, no razonado:** con el blob de 716 salen 200 huérfanas y 198 archivos en riesgo; con el regenerado salen **2** —`restrict_corporate_update_columns` y `sexo_del_viajero`, las dos que de verdad estaban en vuelo esa noche— y **0** archivos en riesgo.

  **El arreglo no fue regenerar la lista, que volvería a pudrirse.** Ahora la genera `scripts/generar-consulta-huerfanas.mjs` leyendo el repo, y `scripts/test-consulta-huerfanas.mjs` falla dentro de `lint` si el blob deja de coincidir con los archivos. La guardia se mutó en los dos sentidos: con el blob viejo sale en rojo nombrando las 198, y al regenerarlo vuelve a verde.

  **La lección: una red de seguridad que se pudre en silencio es peor que no tenerla**, porque se descubre el día que hace falta y ya no hay plan C. Esta llevaba diez días caducada y nadie lo habría sabido hasta el primer desfase real. Es el mismo patrón que la línea base floja de eslint (entrada 23): lo que no se vuelve a medir deja de afirmar lo que dice afirmar.

  **Hallazgo aparte, que no cambia la decisión pero conviene tener escrito:** esta base no es solo ToursRed. Tiene **cinco esquemas** — `public` (153 tablas), `nature_stay` (16), `routesred` (12), `corporate` (11) y `ecosystem` (3) —, y **Bolt construye RoutesRed dentro de ella**. Eso ya costó: las siete políticas que RoutesRed puso sobre `storage.objects` el 27-ago rompieron la subida de archivos **en todos los buckets**, el de gastos de ToursRed incluido —primero con recursión infinita, después con `permission denied for table transport_provider_users`—, porque `storage.objects` es una sola tabla del proyecto y Postgres evalúa **todas** las políticas permisivas, no solo la del bucket que te interesa. Tardó dos semanas en salir y hicieron falta dos migraciones (`20260911080000` y `20260911090000`) para deshacerlo. **No hay forma de acotar `storage.objects` por esquema**, así que mientras Bolt construya aquí, cualquier política que añada se evalúa en cada subida de ToursRed.
